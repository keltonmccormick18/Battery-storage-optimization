# Analysis and the DP
numpy
pandas
scipy
statsmodels
matplotlib
seaborn
duckdb              # data/extract.py only

# Reinforcement learning. The committed results were produced with stable-baselines3 2.9.0,
# sb3-contrib 2.9.0, torch 2.11.0 and Python 3.13 (see results/manifests/*.json).
# Avoid stable-baselines3 2.4.x: it pins numpy<2, which downgrades numpy and breaks the
# TensorFlow/jax stack that torch.utils.tensorboard imports on Colab.
gymnasium>=1.0
stable-baselines3>=2.9,<3
sb3-contrib>=2.9,<3
torch>=2.2
tensorboard
